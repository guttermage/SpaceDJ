// Import your parser logic
const fs = require('fs'); 

// (Temporary simple version of your function for testing)
function xmlParser(xmlRaw) {
    // Paste your current code from image_87b6d7.png here
    // including the new loop we discussed!
}

// Imagine this is a tiny piece of the NASA data
const sampleXML = `<dish><target name="VOYAGER1" power="-150" /></dish>`;

// Run the function
xmlParser(sampleXML);
